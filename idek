//java script
const Discord = require("discord.js")
const fetch = require("node-fetch")
const client = new Discord.client()

// Config imports

// Gets triggered when the client becomes ready to start working
client.on("ready", () => {
    Console.login(`Logged in as ${client.user.tag}!`);
});


// Gets triggered whenever a message is sent/created on any text channel taht the bot is in
client.on("message", msg=> {
 if (mssg.content === "ping"){
   msg.reply("pong")
 }
});

client.login("OTEzMTkyNDIxMzkzODU0NDc0.YZ66tw.EdRgcutFyspC620DkvlzA401VWs");
